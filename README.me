# PPShop

PPShop is a simple Java-based shopping application that demonstrates core e-commerce concepts such as user authentication, product management, shopping cart handling, and basic admin features.  
It was built as a desktop / console project using plain Java (no external frameworks) and simple text files as storage.

---

## Features

### 👥 User Management
- Sign up as a **normal user**
- Log in / log out functionality
- Separate classes for:
  - `normal_user` (customer)
  - `admin_user` (administrator)
  - Base `user` model

### 🔐 Authentication & Security
- Login and signup pages (`loginpage.java`, `signinpage.java`)
- Basic password handling using a hashing utility (`hashing.java`)
- Simple validation logic

### 🛒 Shopping
- Product model and management (`product.java`)
- Product listing on normal user pages (`normalpage.java`, `mypage.java`)
- Add items to cart (`cartpage.java`)
- View shopping history (`hiscard.java`)
- Cart data stored in `cart.txt`

### 🧑‍💻 Admin Panel
- Admin dashboard (`adminpage.java`)
- Create / edit / delete products (`editpage.java`)
- View list of registered users (`showusers.java`)
- Admin profile page (`aprofilepage.java`)

### 💾 Data Storage
- Simple persistence using text files:
  - `ndata.txt` – normal user data
  - `adata.txt` – admin user data
  - `pdata.txt` – product data
  - `cart.txt` – cart data
- `datas.java` and `Arraylist.java` provide helper logic for loading and managing in-memory data structures.

---

## Project Structure

Key files in the repository:

```text
App.java              // Main entry point of the application
user.java             // Base user class
normal_user.java      // Normal (customer) user
admin_user.java       // Admin user

loginpage.java        // Login screen / logic
signinpage.java       // Sign-up screen / logic

normalpage.java       // Main page for normal user
nprofilepage.java     // Normal user profile
mypage.java           // Additional user page (e.g., account / orders)

adminpage.java        // Admin dashboard
aprofilepage.java     // Admin profile
showusers.java        // Admin user list view
editpage.java         // Admin product editing

product.java          // Product model
cartpage.java         // Shopping cart logic / UI
hiscard.java          // Purchase history view

hashing.java          // Password hashing utility
datas.java            // Data loading / saving helpers
Arraylist.java        // Custom list / data container

ndata.txt             // Normal users data
adata.txt             // Admin users data
pdata.txt             // Products data
cart.txt              // Cart / orders data
